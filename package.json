{
  "name": "rigrout",
  "version": "2.0.0",
  "description": "RigRout - Commercial Route Intelligence. Truck stop / restriction data server plus a Leaflet-based route planning front end.",
  "main": "rigrout-server.js",
  "scripts": {
    "start": "node rigrout-server.js",
    "mobile:prepare": "node scripts/prepare-mobile.mjs",
    "mobile:sync": "npm run mobile:prepare && cap sync",
    "mobile:android": "npm run mobile:sync && cap open android",
    "mobile:ios": "npm run mobile:sync && cap open ios"
  },
  "engines": {
    "node": ">=22"
  },
  "author": "",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
