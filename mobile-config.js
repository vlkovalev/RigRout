// Web default. The mobile preparation script writes the deployed API origin
// into www/mobile-config.js without placing provider secrets in the app.
window.RIGROUT_API_BASE = window.RIGROUT_API_BASE || '';
